# CLI Parity Before Rust-Native API

`scenedetect-rs` starts with PySceneDetect CLI parity before designing a broad
Rust-native API because CLI behavior gives agents a concrete public interface
and an existing Reference Oracle. The Rust API remains public and typed, but its
shape should be learned from parity slices before expanding.
