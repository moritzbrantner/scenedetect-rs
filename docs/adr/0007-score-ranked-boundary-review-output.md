# Score-Ranked Boundary Review Output

`scenedetect-rs` keeps `list-scenes` as the chronological Scene List export and
adds `list-boundaries` as a separate Boundary Candidate review output. This
preserves the parity-oriented Scene List contract while giving reviewers a
score-ranked diagnostic artifact for borderline Scene Boundary decisions.
